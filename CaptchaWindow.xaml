<Window x:Class="WpfAppMed.CaptchaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppMed"
        mc:Ignorable="d"
            Title="Проверка капчи" Height="350" Width="400"
    WindowStartupLocation="CenterOwner"
    ResizeMode="NoResize"
    WindowStyle="SingleBorderWindow"
    ShowInTaskbar="False"
    Topmost="True">
    <Grid>
        <Image x:Name="CaptchaImage" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="10,10,10,0" Height="100"/>
        <Button Content="Обновить капчу" Width="120" Height="30" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,275,0,0" Click="RefreshCaptchaButton_Click"/>
        <TextBox x:Name="CaptchaTextBox" Width="200" Height="30" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,175,0,0"/>
        <Button Content="Проверить" Width="100" Height="30" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,224,0,0" Click="VerifyCaptchaButton_Click"/>
    </Grid>
</Window>
